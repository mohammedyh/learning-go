# Switch Statement

When checking a lot of conditions writing a load of `else if` statements, although valid, can feel tedious. Instead, we can use a switch statement that uses alternative syntax and is easier to read and write

Coming from JavaScript, thank goodness we no longer need to write `break` after the `case` and `default` statements.
